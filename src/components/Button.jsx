// Moved to src/ui/Button.jsx as part of the design-system refactor (Wave 1).
// This re-export keeps existing `./Button` imports working; later waves update
// import paths to '../ui/Button' as they touch each file.
export { default } from '../ui/Button';
