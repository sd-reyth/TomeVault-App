# src/ui — Design system primitives

Presentation-only components. No business logic.

Current: `Button`, `Icon`, `Text`, `Card`, `DiceIcon`, `IconButton`, `SegmentedControl`.

Add primitives when they help ship consistent UI faster — not as a prerequisite for visual work.
