# src/features — Feature views

Thin views that receive props and emit events. No direct Firestore.

Sub-folders (added per wave):
- `combat/` — TurnBanner, ParticipantRow, CombatActions, ConditionChips (Wave 2b)
- `handouts/` — HandoutModal, HandoutsView (Wave 4)
- `chat/` — ChatView (Wave 5)
- `inventory/` — InventoryView, WalletSection (Wave 5)
- `landing/` — LandingScreen (Wave 5)
- `settings/` — SettingsModal (Wave 4)
